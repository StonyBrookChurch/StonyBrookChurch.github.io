name: Check for Livestream

on:
  schedule:
    # 8:50 AM Central - before first service (9:00 AM)
    # Two crons to handle DST: one for CDT (summer), one for CST (winter)
    - cron: '50 13 * * SUN'  # 8:50 AM CDT (UTC-5)
    - cron: '50 14 * * SUN'  # 8:50 AM CST (UTC-6)
    # 10:20 AM Central - before second service (10:30 AM)
    - cron: '20 15 * * SUN'  # 10:20 AM CDT (UTC-5)
    - cron: '20 16 * * SUN'  # 10:20 AM CST (UTC-6)
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Set timezone to America/Chicago
        run: |
          sudo timedatectl set-timezone America/Chicago

      - name: Check if we're within the service window
        id: season
        run: |
          CURRENT_MINUTES=$(( 10#$(date +%H) * 60 + 10#$(date +%M) ))
          WINDOW_START=$(( 8 * 60 + 50 ))   # 8:50 AM
          WINDOW_END=$(( 11 * 60 + 30 ))    # 11:30 AM

          echo "Current Central time: $(date +%H:%M) (${CURRENT_MINUTES} min), window: ${WINDOW_START}-${WINDOW_END} min"

          if [[ $CURRENT_MINUTES -ge $WINDOW_START && $CURRENT_MINUTES -lt $WINDOW_END ]]; then
            echo "✅ Within service window (8:50 AM - 11:30 AM Central) - starting poll"
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "⏭️  Outside service window - skipping"
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

      - name: Poll for livestream
        if: steps.season.outputs.should_run == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TIMEOUT=1200     # 20 minutes max
          INTERVAL=30      # Check every 30 seconds
          START_EPOCH=$(date +%s)

          echo "🔍 Starting livestream detection (polling every ${INTERVAL}s for up to ${TIMEOUT}s)"

          while true; do
            CURRENT_EPOCH=$(date +%s)
            ELAPSED=$((CURRENT_EPOCH - START_EPOCH))

            if [[ $ELAPSED -ge $TIMEOUT ]]; then
              echo "❌ Timeout reached after ${ELAPSED}s - no livestream detected"
              exit 1
            fi

            CURRENT_TIME=$(date +%H:%M:%S)

            RESPONSE=$(curl -s "https://www.googleapis.com/youtube/v3/search?part=snippet&channelId=${{ secrets.YOUTUBE_CHANNEL_ID }}&eventType=live&type=video&key=${{ secrets.YOUTUBE_API_KEY }}")
            LIVE_COUNT=$(echo "$RESPONSE" | jq '.pageInfo.totalResults // 0')

            if [[ "$LIVE_COUNT" -gt 0 ]]; then
              echo "🎥 Livestream detected at $CURRENT_TIME!"
              echo "🚀 Triggering deploy workflow..."
              gh workflow run deploy.yml --ref main
              echo "✅ Deploy triggered - exiting"
              exit 0
            fi

            echo "[$CURRENT_TIME] No livestream found (${ELAPSED}s elapsed)"
            sleep $INTERVAL
          done
